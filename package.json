{
  "name": "yehwan-os3",
  "version": "1.0.0",
  "description": "Open Systems 3 online exhibition",
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "deploy": "npm run build && npx wrangler deploy",
    "preview": "npm run build && npx wrangler dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
